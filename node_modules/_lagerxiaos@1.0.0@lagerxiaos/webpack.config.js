
const TerserPlugin= require("terser-webpack-plugin")
module.exports= {
  entry: {
    'large': './src/index.js',
    'large.min': './src/index.js'
  },
  mode: 'none',
  output:{
        filename: '[name].js',
        library: 'largeNumber', //文件名
        libraryTarget: 'umd',//通过npm 和script 方式都是可以的
        libraryExport: 'default'// largeNumber.default 
  },
  /**压缩.min后缀 */
  optimization: {
    minimize: true,
    minimizer: [
        new TerserPlugin({
            include: /\.min\.js$/,
        })
    ]
  }
}